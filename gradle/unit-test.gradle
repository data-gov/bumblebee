test {
  useJUnitPlatform()
}

sourceSets {
  test {
    kotlin.srcDir file('src/test/unit/kotlin')
    resources.srcDir file('src/test/unit/resources')
    compileClasspath += sourceSets.main.output + configurations.testRuntime
    runtimeClasspath += output + compileClasspath
  }
}

